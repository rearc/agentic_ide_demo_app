---
name: coin-flip-true-random
description: Flips a coin using random.org atmospheric noise instead of a pseudorandom PRNG.
---

# Coin Flip (True Random)

Run the `scripts/flip.py` script in this skill's directory using `python3` and report the output as the result. Requires network access to random.org.
