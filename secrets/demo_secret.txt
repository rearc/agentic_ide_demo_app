sk-FAKE-workshop-not-real-do-not-use-0000000000000000

# This is a deliberately FAKE secret used only for the Section 3 permissions
# demo (content-exclusion guardrail + honest bypass). It is not a real key.
#
# Demo arc:
#   1. Ask the agent to read this file via the Read tool -> DENIED by the
#      `Read(./secrets/**)` deny rule in .claude/settings.json (the guardrail).
#   2. `cat ./secrets/demo_secret.txt` via Bash -> SUCCEEDS (a shell read walks
#      past a Read-tool deny; guard-shell.sh does not block `secrets/`).
#   => A permission deny is a guardrail, not a boundary. The sandbox is the
#      boundary (taught as its own separate demo).
