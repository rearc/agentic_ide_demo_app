# Test-only dependencies. Install with:
#   pip install -r requirements-dev.txt
# This pulls in the runtime deps too, so it is the only install a contributor
# running the test suite needs.
-r requirements.txt

pytest==8.4.1
responses==0.25.7
